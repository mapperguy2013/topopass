# Generated map output

`osm-raw.geojson` is the source-of-truth OpenStreetMap export supplied for this
map. `map.svg` is generated directly from it.

Run `npm.cmd run map:build:kings-cross-euston` after replacing the source data
or changing renderer styling. Map data is (c) OpenStreetMap contributors and
available under the Open Database License.
